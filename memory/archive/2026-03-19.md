2026-03-19
- nian-desktop 连接 Gateway 成功，解决了一系列问题：CORS（去掉 HTTP 预检）、client.id 枚举（改为 webchat）、device identity（直接不传）、签名格式（pipe-delimited v2）、operator.read scope（日志需要）
- 重写了 Dashboard 为 5-tab 监控面板（概览/任务/统计/日志/Cron），子 agent 实现
- 修复了聊天不返回消息的 bug：chat streaming 事件中 content 在 payload.message.content 而非 payload.content
- **Ray 提醒：要充分利用团队**——nian-desktop 应该展示和调度整个 agent 团队，不只是聊天客户端
- **device identity 签名方案确定：Ed25519**（不是 secp256k1！走了大弯路）
  - Gateway 的 `verifyDeviceSignature` 用 Ed25519（Node.js `crypto.sign/verify`）
  - `deriveDeviceIdFromPublicKey(pubKey)` = `SHA-256(rawPubKey).hex`（完整 64 字符，无前缀）
  - `normalizeDevicePublicKeyBase64Url` = base64url(32-byte raw Ed25519 pub key)
  - 签名 payload: `v2|{deviceId}|{clientId}|{clientMode}|{role}|{scopes_csv}|{signedAtMs}|{token}|{nonce}`
  - nian-desktop 用浏览器 `crypto.subtle.generateKey({name:'Ed25519'})` 生成密钥
  - 存储到 IndexedDB（从 localStorage 迁移，key 版本 v5）
- **pairing required 解决方案**：Tauri 从 Windows IP (172.25.192.1) 连接，Gateway 只自动批准 localhost
  - 设置了 cron (c036b734) 每 10s 自动批准来自 172.25.192.* 的配对请求
  - IndexedDB 持久化密钥后重启不再生成新密钥
  - ConnectionBanner 添加了配对等待 UI（琥珀色横幅 + 设备 ID）
- **Dashboard 健康状态修复**：health 返回的是复杂嵌套对象（channels/agents/sessions），不能直接 Object.entries 遍历
  - 重写为 HealthSummary 组件，只提取有意义字段：Gateway 状态、频道列表、心跳间隔、Agent 数量、每个频道运行状态
  - cron.schedule 是对象 `{kind, everyMs}` 不是字符串，用 formatSchedule() 格式化
  - cron 的 nextRun 字段在 state.nextRunAtMs，不是直接 nextRun
- nian-desktop 聊天全链路已通：connect → chat.send → 接收 chat event → 聊天正常
- `@noble/secp256k1` 依赖已移除（不再需要），签名改用浏览器原生 Ed25519
- client.id 必须用 `openclaw-control-ui` 才能获得 operator.write scope（chat.send 需要）
- Control UI 源码中的 secp256k1 代码是另一个功能，不是 device identity
