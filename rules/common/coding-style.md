# Coding Style

## 原则

- **不可变优先**：修改返回新对象，不修改原对象
- **小文件**：200-400 行典型，800 行上限；函数 <50 行
- **显式错误处理**：不静默吞错误，每层都要处理
- **输入验证**：系统边界必须校验，使用 schema-based validation
- **命名清晰**：读代码像读散文

## 编码前 Checklist

- [ ] 文件职责单一，不超过 800 行
- [ ] 没有硬编码魔法值（用常量或配置）
- [ ] 没有深层嵌套（>4 层就重构）
- [ ] 错误处理完整
- [ ] 不可变模式（如适用）
